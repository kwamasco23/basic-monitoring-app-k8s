apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
spec:
  type: NodePort
  selector:
    app: monitoring-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30009


---
apiVersion: v1
kind: Service
metadata:
  name: payments-service
spec:
  type: NodePort
  selector:
    app: monitoring-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30009
